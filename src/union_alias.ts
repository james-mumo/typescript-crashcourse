let username: string = "Alex";

let pageName: string | number = "1"; //union using pipe for maeing have mutle types

let errorMessage: string | null = null;

interface UserInterface {
  name: string;
  age: number;
}

let user: UserInterface | null = null;
 
